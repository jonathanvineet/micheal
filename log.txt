# Update and enable universe (if needed)
sudo apt update
sudo apt install -y software-properties-common apt-transport-https ca-certificates curl gnupg

# Optional: enable universe (some packages live there)
sudo add-apt-repository universe
sudo apt update

# Install required binaries
sudo apt install -y \
  ffmpeg \
  imagemagick \
  poppler-utils \   # provides pdftoppm
  ghostscript \
  libreoffice \
  build-essential \
  pkg-config \
  python3 \
  python3-dev \
  git \
  curl \
  libvips-dev       # necessary for sharp (libvips)